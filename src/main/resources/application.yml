spring:
  application:
    name: "cooperfilmes"
  datasource:
    url: jdbc:postgresql://localhost:5432/cooperfilmes
    username: admin
    password: admin123
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: never